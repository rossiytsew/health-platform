CREATE PROCEDURE [dbo].[usp_DeleteReport]
	@ReportId INT
AS
BEGIN
	BEGIN TRANSACTION;

	BEGIN TRY		
		DELETE
		FROM [dbo].[Report]
		WHERE [ReportId] = @ReportId

		COMMIT TRANSACTION;
	END TRY
	BEGIN CATCH
        ROLLBACK TRANSACTION;
		THROW;
    END CATCH
END
